[[source]]
name = "azure"
url = "https://${AIRFLOW_FEED_NAME}:${AIRFLOW_FEED_TOKEN}@pkgs.dev.azure.com/snapmetrix/_packaging/airflow-feed/pypi/simple/"
verify_ssl = true

[dev-packages]

[packages]
"airflow-dags" = {editable = true, path = ".", extras = ["dev", "tests", "docs"]}

[requires]
python_version = "3.7"

[pipenv]
allow_prereleases = true
